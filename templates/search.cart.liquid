{%-layout none-%}
[{%-for product in search.results-%}
{
	"name": "{{product.name}}",
	"product_id": "{{product.id}}",
	"colIds": [{%-for col in product.collections-%}"{{col.id}}"{%-unless forloop.last-%},{%-endunless-%}{%-endfor-%}]
}{%-unless forloop.last-%},{%-endunless-%}
{%-endfor-%}]