/* ============================================================
   MAIN SCSS — (trống có chủ đích)
   Toàn bộ style starter cũ (.collection-*, .product-detail__*, .cart-page*,
   .grid-products, .product-gallery, .cart-summary...) đã được xác nhận KHÔNG
   sử dụng (thay bằng các component .coll__*, .pd__*, .bs_cart__*) — đã gỡ.
   File này không còn được nạp (xem preload-head.liquid). Giữ lại để thêm
   style chung cấp dự án sau này nếu cần.
   ============================================================ */
