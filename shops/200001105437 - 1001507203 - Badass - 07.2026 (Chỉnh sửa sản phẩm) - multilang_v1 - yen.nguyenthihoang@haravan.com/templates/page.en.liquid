{% layout 'en' %}
{%- comment -%} Template: Page — Trang nội dung tĩnh {%- endcomment -%}
<div class="page-content">
  <div class="container">
    <h1 class="page-content__title">{{ page.metafields.hrvmultilang_en.title | append: '' | default: page.title }}</h1>
    <div class="page-content__body content-style-list">
      {{ page.metafields.hrvmultilang_en.description | append: '' | default: page.content }}
    </div>
  </div>
</div>
